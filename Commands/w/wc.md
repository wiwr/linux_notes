# word count

### only lines
```bash
wc -l
```

### only words
```bash
wc -w
```

### only characters
```bash
wc -m
```

### only bytes
```bash
wc -c
```

```bash
ps -ef | wc -l
```

### all files in current folder
```bash
wc -l *
```