### pop-up window
```bash
tmux display-popup
```
or `Ctrl+B` and `:display-popup`
to exit press `Ctrl+C`

can be run program directly
```bash
tmux popup htop
```
or `Ctrl+B` and `:popup htop`

to close popup with `q`
```bash
tmux popup -E htop
```

