`ls -1` - one file per line

`ls -d`

```bash
ls -lS
```

```bash
ls -lt
```

```bash
ls -lt --time=birth
```

```bash
ls -lb
```

```bash
ls -li
```

```bash
find ~ -inum <inode>
```

```bash
ls -lX
```

```bash
ls -l --full-time
```