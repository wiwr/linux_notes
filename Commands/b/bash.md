```bash
bash script.sh
```

`bash -x` **Runs the script normally and prints a live execution trace.**
`bash +n` **Dry-runs the script to check for syntax errors without executing code.**
```bash
bash +n -x script.sh
```